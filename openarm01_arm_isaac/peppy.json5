// Isaac sim implementation of openarm01_arm. Rust node talking to a running
// Isaac Sim via raw peppylib JSON channels published by
// openarm01_robot_initializer_isaac — engine-internal coupling, not part of
// the openarm01_arm:v1 interface contract.
{
  peppy_schema: "node_v1",
  manifest: {
    name: "openarm01_arm_isaac",
    tag: "v1",
    depends_on: {
      nodes: [
        { name: "openarm01_robot_initializer_isaac", tag: "v1", link_id: "sim" },
      ],
    },
  },
  interfaces: {
    conforms_to: [
      { name: "openarm01_arm", tag: "v1" },
      { name: "openarm01_joint_state_source", tag: "v1" },
    ],
  },
  execution: {
    language: "rust",
    container: {
      def_file: "apptainer.def",
    },
    parameters: {
      // The arm_id at runtime (0 = left, 1 = right). Two instances are
      // expected; the bin's file-lock + arm_id guard rejects collisions.
      arm_id: "u8",
    },
  },
}
