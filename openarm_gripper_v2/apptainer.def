Bootstrap: docker
From: tuatini/peppy-rust-cargo-base

%files
    . /opt/openarm_gripper_v2

%post
    set -eux
    cd /opt/openarm_gripper_v2
    cargo build --release

%runscript
    cd /opt/openarm_gripper_v2
    exec ./target/release/openarm_gripper_v2 "$@"
