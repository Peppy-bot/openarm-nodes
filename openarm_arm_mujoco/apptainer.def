Bootstrap: docker
From: tuatini/peppy-rust-cargo-base:latest

%labels
    Name openarm-arm-mujoco
    Version v1

%files
    . /opt/openarm_arm_mujoco

%post
    set -eux
    export DEBIAN_FRONTEND=noninteractive

    cd /opt/openarm_arm_mujoco
    cargo build --release

%runscript
    cd /opt/openarm_arm_mujoco
    exec ./target/release/openarm_arm_mujoco "$@"
