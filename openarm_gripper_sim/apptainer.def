Bootstrap: docker
From: tuatini/peppy-rust-cargo-base:latest

%labels
    Name openarm-gripper-sim
    Version v1

%files
    . /opt/openarm_gripper_sim

%post
    set -eux
    export DEBIAN_FRONTEND=noninteractive

    cd /opt/openarm_gripper_sim
    cargo build --release

%runscript
    cd /opt/openarm_gripper_sim
    exec ./target/release/openarm_gripper_sim "$@"
