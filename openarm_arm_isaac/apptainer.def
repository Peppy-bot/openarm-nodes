Bootstrap: docker
From: tuatini/peppy-rust-cargo-base:latest

%labels
    Name openarm-arm-isaac
    Version v1

%files
    . /opt/openarm_arm_isaac

%post
    set -eux
    export DEBIAN_FRONTEND=noninteractive

    cd /opt/openarm_arm_isaac
    cargo build --release

%runscript
    cd /opt/openarm_arm_isaac
    exec ./target/release/openarm_arm_isaac "$@"
